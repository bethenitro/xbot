# Browser Engines (choose one or more)
camoufox>=0.4.0  # Firefox-based stealth browser
# patchright>=1.0.0  # Chromium-based stealth browser (requires: patchright install chrome)
zendriver  # Chrome DevTools Protocol-based automation (no WebDriver) - ACTIVE

# Core dependencies
playwright>=1.40.0
aiohttp>=3.9.0
requests>=2.31.0
pysocks>=1.7.1
pillow>=10.0.0

# Web-based GUI
flask>=2.3.0
flask-cors>=4.0.0
flask-socketio>=5.3.0

# Note: For browser support:
# Patchright: pip install patchright && patchright install chrome
# ZenDriver: pip install zendriver